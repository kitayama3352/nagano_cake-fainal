

<div class="row">
  <div class="col-md-10 mx-auto">
     <div class="row">
    <h4 class="col-md-10 mb-5 font-weight-bold">会員一覧</h4>
    </div>
   <table bgcolor="#FFF" class="table table-bordered shadow">
    <thead  class="bg-secondary text-light">
        <tr>
          <th>会員ID</th>
          <th>氏名</th>
          <th>メールアドレス</th>
          <th>ステータス</th>
        </tr>
      </thead>
       <tbody>
        <% @customers.each do |customer| %>
          <tr>
            <td>
              <%= customer.id %>
            </td>
            <td>
              <%= link_to admins_customer_path(customer), class: "text-secondary"  do %>
                <i></i>
                <%= customer.last_name %>
                <%= customer.first_name %>
              <% end %>
            </td>
            <td>
              <%= customer.email %>
            </td>
            <td>
              <% if customer.cancel_flag == false %>
                <i>退会</i>
              <% else %>
                <i class="true">有効</i>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= paginate @customers %>
  </div>
</div>

